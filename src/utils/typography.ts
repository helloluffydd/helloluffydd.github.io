import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.666,
  headerFontFamily: ['GenWanMin TW SemiBold', 'Nanum Gothic'],
  bodyFontFamily: ['GenSekiGothic Regular', 'Nanum Gothic'],
});

export default typography;
